// The workbench view model — the BFF↔frontend shapes, generated by protobuf-es from
// schema/proto/themis/workbench/models/workbench.proto. App code imports the message
// types and their schemas from here rather than the generated path; the BFF serves proto3-JSON
// (toJson) and the client parses it (fromJson), so one codec speaks each side. The oneof
// `ConversationEvent.kind` and the `DocumentResponse.document` presence carry the variant
// invariants structurally — there is no hand-written validator or projection layer.

export * from "@/gen/themis/workbench/models/workbench_pb";
