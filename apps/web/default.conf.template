# Rendered by the nginx image entrypoint (envsubst) at startup; Cloud Run
# injects $PORT.
server {
    listen       ${PORT};
    server_name  _;

    location / {
        root  /usr/share/nginx/html;
        index index.html;
    }
}
