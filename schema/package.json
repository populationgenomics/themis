{
  "name": "themis-schema",
  "version": "0.0.0",
  "private": true,
  "description": "TypeSpec schema sources and the emitters that generate JSON Schema and Zod from them.",
  "type": "module",
  "scripts": {
    "compile": "tsp compile",
    "smoke:zod": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@typespec/compiler": "1.13.0",
    "@typespec/json-schema": "1.13.0",
    "typespec-zod": "0.0.0-68"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "zod": "4.4.3"
  }
}
