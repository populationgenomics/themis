{
  "name": "themis-schema",
  "version": "0.0.0",
  "private": true,
  "description": "TypeSpec schema sources and the emitters that generate JSON Schema (and, later, Zod) from them.",
  "type": "module",
  "scripts": {
    "compile": "tsp compile"
  },
  "dependencies": {
    "@typespec/compiler": "1.13.0",
    "@typespec/json-schema": "1.13.0"
  }
}
