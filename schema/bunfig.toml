# Supply-chain age gate: refuse dependency versions published less than 7 days
# ago, so a compromised fresh release is filtered before it can be installed
# (the repo-wide policy; matches apps/web). Applies to direct and transitive deps.
[install]
minimumReleaseAge = 604800  # 7 days, in seconds
