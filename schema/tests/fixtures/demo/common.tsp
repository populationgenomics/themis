// Types shared across the demo domain. Split out so the cross-file ref
// from widget.tsp exercises the bundler's multi-file resolution.

enum Colour {
  red: "red",
  green: "green",
  blue: "blue",
}
