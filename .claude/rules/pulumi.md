---
paths:
  - "infra/**/*"
---

`infra/` is a Pulumi program. Before writing or modifying it, load the Pulumi skills:
`pulumi:pulumi-overview` to orient, then the relevant one — `pulumi:pulumi-best-practices`,
`pulumi:pulumi-component`, `pulumi:provider-upgrade`, etc.
